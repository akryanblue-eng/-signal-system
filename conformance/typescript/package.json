{
  "name": "nic-conformance-ts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Independent TypeScript port of NIC v1.1's deterministic core (CanonicalPath, CanonicalURL, ProofV1 verification, set_hash, witness_hash), verified against the language-agnostic golden corpus.",
  "scripts": {
    "build": "tsc -p .",
    "conformance": "npm run build && node dist/src/runCorpus.js",
    "test": "npm run build && node --test dist/tests/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^26.0.0"
  }
}
