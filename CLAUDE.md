# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This repository is newly initialized and contains no source code yet. The only file present beyond git metadata is a stub `README.md`. This CLAUDE.md should be updated as the project takes shape.

## Repository

- **Name:** -signal-system
- **Remote:** `akryanblue-eng/-signal-system` on GitHub
- **Default branch:** `main`
- **Feature branches:** follow the pattern `claude/<descriptor>-<hash>` (e.g. `claude/claude-md-docs-hEEvF`)

## Next Steps for This File

Once the project stack is chosen and code is added, update this file with:
- Build, lint, and test commands (including how to run a single test)
- Architecture overview — data flow, key abstractions, module boundaries
- Any non-obvious conventions (naming, file layout, state management patterns)
- Environment setup (required env vars, local service dependencies)
