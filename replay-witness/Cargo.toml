[package]
name = "replay-witness"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "replay-witness"
path = "src/main.rs"

[dependencies]
dsvm-core = { path = "../impl_c" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hex = "0.4"
sha2 = "0.10"
