{
  "name": "candidate_digest_contract",
  "version": "1.0",
  "authority": "derived",
  "source": "cvp_transition/witness.py:compute_candidate_digest",
  "law": "digest = sha256(json.dumps(candidate_spec, sort_keys=True).encode('utf-8'))",
  "hash_algorithm": "sha256",
  "serialization": {
    "format": "json",
    "key_ordering": "sorted",
    "encoding": "utf-8",
    "null_field_inference": false
  },
  "excluded_fields": [
    "independent_execution"
  ],
  "rationale": "Witness material is excluded by type, not convention. Any presence of witness keys in the digest domain is a hard law violation. Identity must be computable without any attestation existing.",
  "invariant": "digest_value_must_not_change_when_independent_execution_is_mutated"
}
